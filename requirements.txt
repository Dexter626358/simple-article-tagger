# Основные зависимости для извлечения метаданных из Word документов

# Для работы с DOCX файлами
python-docx>=1.1.0

# Для работы с RTF файлами (опционально, для извлечения текста)
striprtf>=0.0.26

# Для веб-интерфейса
flask>=2.3.0

# Для конвертации DOCX в HTML
mammoth>=1.6.0

# Для работы с PDF файлами (опционально, для конвертации PDF в HTML)
# Установите одну из библиотек:
pdfplumber>=0.9.0  # Предпочтительно: лучше сохраняет структуру документа
# pymupdf>=1.23.0  # Альтернатива: быстрее, но может хуже сохранять структуру

# Для конвертации PDF в изображения (для pdf_text_area_selector.py)
pdf2image>=1.16.0
Pillow>=10.0.0

# Для работы с GPT API
openai>=1.0.0

# Тесты
pytest>=8.2.0

# WSGI server for production
gunicorn>=21.2.0
