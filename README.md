# Word Parser Metadata

Скрипт для извлечения метаданных из Word документов (DOCX, RTF) и сохранения результатов в JSON формате.

## Возможности

- Извлечение метаданных из DOCX файлов (автор, дата создания, тема, ключевые слова и т.д.)
- Поддержка RTF файлов
- Обработка одного файла или целой директории
- Сохранение результатов в JSON формате
- Рекурсивная обработка поддиректорий

## Установка

1. Создайте виртуальное окружение:
```bash
python -m venv venv
```

2. Активируйте виртуальное окружение:

**Windows (cmd):**
```cmd
venv\Scripts\activate
```

**Windows (PowerShell):**
```powershell
venv\Scripts\Activate.ps1
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Использование

### Автоматическое извлечение метаданных

#### Обработка одного файла

```bash
python extract_metadata.py path/to/document.docx
```

#### Обработка директории

```bash
python extract_metadata.py words_input/
```

#### Сохранение результатов в файл

```bash
python extract_metadata.py words_input/ -o output.json
```

#### Рекурсивная обработка поддиректорий

```bash
python extract_metadata.py words_input/ -r -o output.json
```

### Интерактивное извлечение метаданных

#### 1. Веб-интерфейс (рекомендуется) 🌐

Самый удобный способ - открыть файл в браузере и выделять метаданные мышкой:

```bash
python web_extract.py
```

**Как это работает:**

1. Скрипт запускает локальный веб-сервер
2. Браузер открывается автоматически (или откройте `http://localhost:5000`)
3. В левой панели отображается текст документа построчно
4. Кликайте на строки для выделения - они автоматически заполнят активное поле
5. Можно редактировать текст в полях вручную
6. Есть поиск по тексту для быстрого нахождения нужных строк
7. Нажмите "Сохранить" - метаданные сохранятся в JSON файл

**Преимущества:**
- ✅ Визуальный интерфейс
- ✅ Выделение мышкой
- ✅ Поиск по тексту
- ✅ Удобное редактирование
- ✅ Автоматическое сохранение

#### 2. Консольный интерактивный режим

Для работы в терминале:

```bash
python interactive_extract.py words_input/ПММ-6_1.rtf
```

**Как это работает:**

1. Скрипт извлекает текст из документа и показывает его построчно
2. Для каждого поля метаданных (название, аннотация, ключевые слова и т.д.) вы можете:
   - Выбрать одну строку: `5`
   - Выбрать диапазон строк: `5-10`
   - Выбрать несколько строк: `5,7,9`
   - Комбинировать: `5-8,12`
   - Пропустить поле: `skip` или `s`
   - Просмотреть строку: `view 50` или `v 50`
   - Показать список строк: `list 30` или `l 30`
   - Найти текст: `search заголовок`

3. Результаты автоматически сохраняются в JSON файл

**Пример использования:**

```
Интерактивное извлечение метаданных
Файл: ПММ-6_1.rtf
================================================================================

Извлечение текста из файла...
Извлечено строк: 150

================================================================================
Название статьи (русский) (title)
================================================================================

Первые строки документа:
   1: УДК 62-752
   2: МОДЕЛИРОВАНИЕ ЭФФЕКТА "ЗАСТРЕВАНИЯ" МАЯТНИКА
   3: НА ВРАЩАЮЩЕМСЯ ВАЛУ МЕХАНИЧЕСКОЙ СИСТЕМЫ
   4: С ДВУМЯ СТЕПЕНЯМИ СВОБОДЫ

Введите выбор для 'Название статьи (русский) (title)': 2-4
```

Этот режим особенно полезен для:
- Создания обучающих данных для улучшения автоматического извлечения
- Обработки документов с нестандартной структурой
- Точной разметки метаданных для важных документов

## Формат выходных данных

Результаты сохраняются в JSON формате со следующей структурой (соответствует формату статей):

```json
[
  {
    "title": "Название статьи на русском",
    "title_en": "Article title in English",
    "doi": "10.33266/1024-6177-2023-68-1-5-14",
    "annotation": "Аннотация статьи на русском языке",
    "annotation_en": "Article annotation in English",
    "keywords": "ключевые, слова, на русском",
    "keywords_en": "keywords, in, english",
    "references_ru": [],
    "references_en": [],
    "year": 2023,
    "issue": 1,
    "article_number": 1,
    "url": "https://example.com/article",
    "url_en": "https://example.com/en/article",
    "pages": "5-14",
    "udc": null,
    "received_date": "20.09.2022",
    "reviewed_date": null,
    "accepted_date": "25.11.2022",
    "funding": "Информация о финансировании"
  }
]
```

**Примечание:** Не все поля могут быть извлечены из каждого документа. Поля, которые не удалось найти, будут иметь значение `null`. Для RTF файлов извлечение метаданных ограничено, так как этот формат не поддерживает стандартные метаданные Word.

## Поддерживаемые форматы

- **DOCX** - полная поддержка метаданных
- **RTF** - базовая поддержка (метаданные могут быть ограничены)
- **DOC** - базовая поддержка (требуются дополнительные библиотеки для полной поддержки)

### 3. Выделение областей в PDF (для текстовых PDF из InDesign) 📄

Для работы с текстовыми PDF файлами (например, из InDesign) можно использовать графический интерфейс для выделения областей и извлечения текста:

```bash
python pdf_text_area_selector.py
```

**Как это работает:**

1. Нажмите "📁 Открыть PDF" и выберите PDF файл
2. Выделите область мышью (зажмите левую кнопку и перетащите)
3. Нажмите "📝 Извлечь текст" для извлечения текста из выделенных областей
4. Текст будет извлечен напрямую из PDF (без OCR, для текстовых PDF)
5. Нажмите "💾 Сохранить координаты" для сохранения координат bbox в JSON

**Особенности:**

- ✅ Работает с текстовыми PDF (из InDesign, LaTeX и т.д.)
- ✅ Прямое извлечение текста (без OCR)
- ✅ Сохранение координат bbox для каждой области
- ✅ Поддержка нескольких выделений на странице
- ✅ Навигация по страницам

**Требования:**

- `pdfplumber` - для извлечения текста
- `pdf2image` и `Pillow` (опционально) - для отображения PDF как изображения
- `poppler` (опционально) - для pdf2image

**Пример сохраненных координат:**

```json
{
  "pdf_file": "path/to/article.pdf",
  "total_pages": 10,
  "selections": [
    {
      "page": 0,
      "bbox": {
        "x1": 100.5,
        "y1": 200.3,
        "x2": 500.8,
        "y2": 350.1
      },
      "text": "Извлеченный текст из области"
    }
  ]
}
```

## Структура проекта

```
word_parser_metadata/
├── extract_metadata.py          # Основной скрипт
├── metadata_extractor.py         # Модуль для извлечения метаданных
├── pdf_text_area_selector.py     # Графический интерфейс для выделения областей в PDF
├── pdf_bbox_helper.py            # Утилита для поиска блоков в PDF по ключевым словам
├── requirements.txt              # Зависимости проекта
├── README.md                     # Документация
├── .gitignore                   # Игнорируемые файлы
└── words_input/                 # Директория с входными файлами
```

## Примечания

- RTF файлы могут не содержать стандартных метаданных Word
- Для полной поддержки DOC файлов могут потребоваться дополнительные библиотеки (olefile, oletools)
- Все даты сохраняются в формате ISO 8601

